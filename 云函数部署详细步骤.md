# 云函数部署详细步骤（图文版）

## ⚠️ 如果右键菜单只显示"不上传 node_modules"

这说明**云开发环境还没有初始化**。

## 📋 完整步骤

### 第一步：开通云开发

#### 1.1 找到云开发按钮
```
微信开发者工具顶部工具栏
[项目] [工具] [调试] [云开发] ← 点击这里
```

#### 1.2 开通云开发
- 如果是第一次使用，会弹出"开通云开发"提示
- 点击【开通】按钮
- 填写环境信息：
  - 环境名称：`prod-env`（或任意名称）
  - 环境 ID：自动生成
  - 选择套餐：【免费版】
- 点击【创建】

#### 1.3 等待初始化
```
创建中...
├─ 创建环境
├─ 初始化数据库
├─ 初始化存储
└─ 初始化云函数
完成！（约 1-2 分钟）
```

### 第二步：验证云开发已开通

#### 2.1 查看云开发控制台
点击顶部的【云开发】按钮后，应该看到：
```
┌─────────────────────────────────────┐
│  云开发控制台                        │
├─────────────────────────────────────┤
│  [数据库] [存储] [云函数] [统计]     │
│                                      │
│  环境：prod-env-xxxxx                │
│  状态：运行中 ✅                      │
└─────────────────────────────────────┘
```

#### 2.2 检查项目目录
在左侧目录树中，`cloudfunctions` 文件夹图标应该变成**云朵图标** ☁️

### 第三步：上传云函数

#### 方法 A：右键菜单（推荐）

1. 在左侧目录树中找到：
   ```
   cloudfunctions ☁️
   └── checkImage 📁
   ```

2. **右键点击** `checkImage` 文件夹

3. 应该看到菜单：
   ```
   ┌──────────────────────────────────┐
   │ 在终端中打开                      │
   │ ─────────────────────────────── │
   │ 上传并部署：云端安装依赖 ✅       │  ← 选这个！
   │ 上传并部署：不上传 node_modules   │
   │ 下载云端安装的依赖                │
   │ ─────────────────────────────── │
   │ 云函数增量上传：上传文件           │
   │ 云函数增量上传：上传文件夹         │
   │ ─────────────────────────────── │
   │ 同步云函数列表                    │
   │ 新建 Node.js 云函数              │
   │ 删除云函数                        │
   └──────────────────────────────────┘
   ```

4. 选择 **"上传并部署：云端安装依赖"**

5. 等待上传完成：
   ```
   上传中...
   ├─ 打包文件
   ├─ 上传到云端
   ├─ 安装依赖包
   └─ 部署完成 ✅
   （约 30-60 秒）
   ```

6. 控制台会显示：
   ```
   [云函数] [checkImage] 上传成功
   [云函数] [checkImage] 部署成功
   ```

#### 方法 B：云开发控制台

1. 点击顶部【云开发】按钮

2. 点击【云函数】标签

3. 点击【新建云函数】（如果是第一次）
   - 或者找到现有的 `checkImage`，点击【删除】后重新创建

4. 填写信息：
   - 云函数名称：`checkImage`
   - 运行环境：Node.js 16
   - 超时时间：20 秒 ⚠️ 重要！

5. 点击【确定】

6. 点击【上传代码】
   - 选择 `cloudfunctions/checkImage` 文件夹
   - 确认上传

### 第四步：验证部署

#### 4.1 检查云函数列表

在云开发控制台 → 云函数标签页：
```
┌────────────────────────────────────────┐
│ 云函数列表                              │
├────────┬─────────┬──────────┬─────────┤
│ 名称   │ 运行环境 │ 超时时间  │ 状态    │
├────────┼─────────┼──────────┼─────────┤
│checkImage│Node.js16│ 20秒    │ 部署成功 ✅│
└────────┴─────────┴──────────┴─────────┘
```

#### 4.2 查看云函数详情

点击 `checkImage` 函数名，检查：
- ✅ 状态：运行中
- ✅ 超时时间：20 秒（不是 3 秒）
- ✅ 代码：显示最新版本
- ✅ 配置：显示 openapi 权限

#### 4.3 测试云函数

在云函数详情页，可以点击【测试】按钮：

测试参数：
```json
{
  "fileID": "cloud://test-xxx.png"
}
```

预期返回：
```json
{
  "success": true,
  "safe": true,
  "message": "图片内容安全"
}
```

### 第五步：测试小程序

#### 5.1 重新编译
- 按 `Ctrl+B`（Windows）或 `Cmd+B`（Mac）
- 或点击【编译】按钮

#### 5.2 测试功能
1. 点击【选择照片】按钮
2. 选择 2-3 张图片
3. 观察：
   - ✅ 图片立即显示
   - ✅ 顶部显示 "🔍 检测中 1/3"
   - ✅ 不再出现超时错误
   - ✅ 检测完成后徽章消失

#### 5.3 查看日志

控制台应该显示：
```
=== 开始图片安全检测 ===
步骤1：下载图片... (1200ms)
步骤2：检测API... (3500ms)
=== 总耗时 4800ms ===
✅ 图片安全
```

## 🔧 常见问题

### Q1: 点击"云开发"提示"appid 不存在"
**A**: 
1. 检查 `project.config.json` 中的 `appid` 是否正确
2. 确认该 appid 已注册并实名认证
3. 确认该 appid 已开通云开发权限

### Q2: 找不到"云开发"按钮
**A**: 
1. 更新微信开发者工具到最新版本（Stable 1.06+）
2. 检查是否是企业版/体验版工具
3. 检查顶部工具栏，可能在【...更多】菜单中

### Q3: 云开发控制台显示"环境异常"
**A**: 
1. 检查账号状态是否正常
2. 检查云开发套餐是否过期
3. 等待 5-10 分钟后重试
4. 联系微信客服

### Q4: 右键菜单还是没有"云端安装依赖"选项
**A**: 
1. 关闭开发者工具，重新打开
2. 检查 `project.config.json` 中是否有：
   ```json
   "cloudfunctionRoot": "cloudfunctions/"
   ```
3. 尝试手动在云开发控制台上传
4. 更新开发者工具到最新版本

### Q5: 部署时提示"找不到 config.json"
**A**: 
确保文件存在且格式正确：
```bash
ls -la cloudfunctions/checkImage/config.json
cat cloudfunctions/checkImage/config.json
```

应该显示：
```json
{
  "permissions": {
    "openapi": [
      "security.imgSecCheck"
    ]
  },
  "timeout": 20
}
```

## 📞 获取帮助

如果按照以上步骤仍无法解决：

1. **截图提供以下信息**：
   - 微信开发者工具版本
   - project.config.json 内容
   - 云开发控制台截图
   - 右键菜单截图
   - 完整错误信息

2. **联系方式**：
   - 邮箱：312827559@qq.com
   - 描述详细操作步骤

## 🎯 核心要点

1. ✅ **必须先开通云开发**
2. ✅ **确认云开发环境运行中**
3. ✅ **选择"云端安装依赖"选项**
4. ✅ **等待部署完成**
5. ✅ **重新编译测试**

---

**最后更新**：2025-11-07  
**适用版本**：微信开发者工具 Stable 1.06+


# 内容安全检测功能 - 完整部署指南

## 📋 已完成的工作

✅ 配置云开发环境  
✅ 创建云函数实现图片内容安全检测  
✅ 修改小程序代码调用云函数检测图片  
✅ 添加错误提示和用户引导  

## 🚀 部署步骤（重要！）

### 步骤1：开通云开发（5分钟）

1. 打开微信开发者工具
2. 点击顶部工具栏的 **"云开发"** 按钮
3. 如果是第一次使用：
   - 点击 **"开通"**
   - 填写环境名称（如：prod-env）
   - 选择套餐（免费版即可，后续可升级）
   - 等待环境创建完成（约1-2分钟）

### 步骤2：上传云函数（3分钟）

1. 在开发者工具左侧目录树中，找到 `cloudfunctions` 文件夹
2. 展开后可以看到 `checkImage` 文件夹
3. **右键点击** `checkImage` 文件夹
4. 选择 **"上传并部署：云端安装依赖"**
5. 等待上传完成，控制台会显示 "上传成功"

> ⚠️ 注意：必须选择"云端安装依赖"，不要选择"不包含node_modules"

### 步骤3：验证部署（2分钟）

1. 在云开发控制台中，点击 **"云函数"** 标签
2. 确认可以看到 `checkImage` 函数，状态为"部署成功"
3. 点击函数名称，可以查看函数详情

### 步骤4：测试功能（3分钟）

1. 重新编译小程序（Ctrl+B 或 Cmd+B）
2. 点击 **"选择照片"** 按钮
3. 选择1-2张测试图片
4. 观察是否显示 "正在检测图片..." 的加载提示
5. 检查控制台日志，确认云函数调用成功

## 📝 功能说明

### 用户体验

- **无感知检测**：选择图片后自动检测，用户无需额外操作
- **智能过滤**：违规图片自动过滤，不影响其他正常图片使用
- **友好提示**：检测过程显示进度，结果清晰提示
- **容错机制**：检测失败时自动降级，不影响正常使用

### 技术实现

```
用户选择图片
    ↓
上传到云存储（临时）
    ↓
调用云函数检测
    ↓
cloud.openapi.security.imgSecCheck
    ↓
返回检测结果
    ↓
删除临时文件
    ↓
过滤违规图片 / 允许使用
```

## ⚠️ 重要注意事项

### 1. 云开发配额

免费版配额（每月）：
- 云函数调用：10万次
- 云存储容量：5GB
- 云存储下载流量：5GB

**建议**：根据实际使用量，考虑升级到付费版

### 2. 成本控制

每张图片检测消耗：
- 1次云存储上传
- 1次云函数调用
- 1次内容安全API调用
- 1次云存储删除

**优化建议**：
- 限制用户一次选择图片数量（当前20张）
- 压缩大图片后再上传检测
- 缓存已检测过的图片结果

### 3. 审核要求

- 内容安全检测是微信平台的**强制要求**
- 必须在 **2025-11-14 09:19:26 之前**完成整改
- 部署完成后，建议立即提交审核

## 🔧 故障排查

### 问题1：云函数上传失败

**可能原因**：
- 网络问题
- 云开发环境未创建
- 开发者工具版本过低

**解决方案**：
1. 检查网络连接
2. 确认云开发环境已创建
3. 更新开发者工具到最新版本

### 问题2：调用云函数失败

**可能原因**：
- 云开发未初始化
- 环境ID配置错误
- 云函数权限未配置

**解决方案**：
1. 检查 `app.js` 中的云开发初始化代码
2. 确认 `checkImage/config.json` 中配置了正确的权限
3. 重新上传云函数

### 问题3：检测一直提示异常

**可能原因**：
- 云开发配额用尽
- 图片格式不支持
- 网络超时

**解决方案**：
1. 检查云开发控制台的配额使用情况
2. 确认图片格式为 jpg/png
3. 增加超时时间或重试机制

## 📱 提交审核

部署完成后：

1. 在微信公众平台的提醒中，点击 **"已完成整改"**
2. 说明已接入内容安全检测API
3. 提交审核，等待平台验证

## 📞 技术支持

如有问题，请联系：
- 邮箱：312827559@qq.com
- 小程序内的"作者联系方式"

## 📚 相关文档

- [微信云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- [内容安全API文档](https://developers.weixin.qq.com/miniprogram/dev/OpenApiDoc/sec-center/sec-check/imgSecCheck.html)
- [云函数开发指南](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/functions.html)

---

**最后更新时间**：2025-11-07
**版本**：v1.0.0


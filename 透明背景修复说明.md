# 透明背景导出修复说明

## 🐛 问题描述

用户反馈：关闭"外部背景"开关后，导出的图片仍然有白色背景，而不是透明背景。

## ✅ 修复内容

### 修改的文件

1. **pages/index/index.js** - 修改了 `drawToCanvas` 函数
2. **pages/frame/frame.js** - 原本就是正确的（无需修改）

### 具体修改

**修改前（index.js）：**
```javascript
drawToCanvas({ canvas, ctx, outW, outH, imgPath, borderPx, zoom, enableOuterBg, outerBgColor, enableInnerBorder, innerBorderColor }){
  return new Promise((resolve)=>{
    if(enableOuterBg){
      ctx.fillStyle = outerBgColor || '#FFFFFF';
      ctx.fillRect(0,0,outW,outH);
      ctx.strokeStyle = '#e5e5e5';
      ctx.lineWidth = 1;
      ctx.strokeRect(0.5,0.5,outW-1,outH-1);
    } else {
      ctx.clearRect(0,0,outW,outH);
    }
    // ...
  });
}
```

**修改后：**
```javascript
drawToCanvas({ canvas, ctx, outW, outH, imgPath, borderPx, zoom, enableOuterBg, outerBgColor, enableInnerBorder, innerBorderColor }){
  return new Promise((resolve)=>{
    // 先清除整个 canvas，确保透明背景
    ctx.clearRect(0, 0, outW, outH);
    
    // 只有在启用外部背景时才绘制背景色
    if(enableOuterBg){
      ctx.fillStyle = outerBgColor || '#FFFFFF';
      ctx.fillRect(0,0,outW,outH);
      ctx.strokeStyle = '#e5e5e5';
      ctx.lineWidth = 1;
      ctx.strokeRect(0.5,0.5,outW-1,outH-1);
    }
    // ...
  });
}
```

### 修复原理

1. **无论是否启用外部背景，都先执行 `clearRect`**
   - 确保 canvas 从透明状态开始
   - 清除之前可能存在的任何内容

2. **只在启用外部背景时才填充背景色**
   - 如果 `enableOuterBg = false`，canvas 保持透明
   - 如果 `enableOuterBg = true`，填充指定的背景色

3. **导出格式自动切换**
   - `enableOuterBg = true`：导出 JPG（不支持透明）
   - `enableOuterBg = false`：导出 PNG（支持透明）

## 🧪 测试步骤

### 测试1：关闭外部背景

1. 打开小程序，选择一张图片
2. **关闭"外部背景"开关**
3. 调整边框和缩放参数
4. 点击"批量导出并保存"
5. 打开相册查看保存的图片
6. **预期结果**：图片应该是 PNG 格式，背景透明（在支持透明背景的查看器中显示为透明）

### 测试2：开启外部背景

1. 选择一张图片
2. **开启"外部背景"开关**
3. 选择背景颜色（如红色、蓝色）
4. 点击"批量导出并保存"
5. 打开相册查看保存的图片
6. **预期结果**：图片应该是 JPG 格式，有选定颜色的背景

### 测试3：切换测试

1. 导出一张关闭外部背景的图片（透明）
2. 导出一张开启外部背景的图片（有背景色）
3. 对比两张图片，确认区别

## 📱 如何查看透明背景

### 在手机相册中

- **iOS**：相册中 PNG 透明背景会显示为棋盘格或纯色（取决于 iOS 版本）
- **Android**：部分相册 APP 会显示为棋盘格或黑色/白色背景

### 发送到其他应用

- 将图片发送到微信、QQ 等支持透明背景的应用
- 在聊天界面中，透明背景图片会融入聊天背景

### 使用图片编辑器

- 使用支持图层的图片编辑器（如 Snapseed、美图秀秀）
- 打开导出的 PNG 图片
- 透明区域应该显示为透明图层或棋盘格

## ⚠️ 注意事项

1. **JPG 不支持透明背景**
   - 开启外部背景时，导出的是 JPG 格式
   - JPG 格式不支持透明，会自动填充为白色或指定的背景色

2. **PNG 文件体积更大**
   - 关闭外部背景时，导出的 PNG 文件通常比 JPG 大
   - 这是正常现象，透明信息需要额外存储空间

3. **部分应用可能不支持透明背景**
   - 某些老旧的图片查看器可能将透明背景显示为白色或黑色
   - 这不是 bug，是查看器的问题

## 🎨 建议使用场景

### 适合使用透明背景（关闭外部背景）

- 需要在其他背景上叠加的图片
- 制作贴纸、表情包
- 需要后期合成的素材
- 去除原图背景后的人像或物品

### 适合使用有色背景（开启外部背景）

- 最终成品，不需要再编辑
- 需要特定颜色背景的作品
- 文件体积要求较小的场景
- 打印输出的图片

## 📞 反馈

如果修复后仍有问题，请联系：
- 邮箱：312827559@qq.com
- 小程序内"作者联系方式"

---

**修复时间**：2025-11-07  
**版本**：v1.1.0


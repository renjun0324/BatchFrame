# 🔧 修复云函数超时问题

## ❌ 问题描述

错误信息：
```
cloud.callFunction:fail Error: errCode: -504003
errMsg: Invoking task timed out after 3 seconds
```

**原因**：云函数默认超时时间只有 3 秒，但图片检测需要 5-10 秒。

## ✅ 已完成的修复

### 1. **增加云函数超时时间**
修改了 `cloudfunctions/checkImage/config.json`：
```json
{
  "permissions": {
    "openapi": [
      "security.imgSecCheck"
    ]
  },
  "timeout": 20  ← 新增：20秒超时
}
```

### 2. **优化云函数日志**
在 `cloudfunctions/checkImage/index.js` 中添加了：
- 详细的执行日志
- 时间统计（每个步骤的耗时）
- 错误堆栈追踪
- 更好的错误提示

### 3. **添加重试机制**
在 `utils/contentSecurity.js` 中：
- 自动重试 2 次（共 3 次尝试）
- 超时后延迟重试（1秒、2秒递增）
- 智能判断是否需要重试

## 🚀 必须执行的步骤

### ⚠️ 重要：必须重新部署云函数！

配置文件修改后，**必须重新上传云函数**才能生效！

#### 步骤：

1. **打开微信开发者工具**

2. **找到云函数目录**
   - 左侧目录树 → `cloudfunctions` → `checkImage`

3. **右键点击 `checkImage` 文件夹**

4. **选择"上传并部署：云端安装依赖"**
   - ⚠️ 必须选择"云端安装依赖"
   - ⚠️ 不要选择"不包含 node_modules"

5. **等待部署完成**
   - 控制台会显示"上传成功"
   - 通常需要 30-60 秒

6. **验证部署**
   - 打开云开发控制台
   - 点击"云函数"标签
   - 确认 `checkImage` 显示"部署成功"
   - 查看配置中的超时时间是否为 20 秒

7. **重新编译小程序**
   - Ctrl+B 或 Cmd+B
   - 确保使用最新代码

8. **测试功能**
   - 点击"选择照片"
   - 选择 2-3 张图片
   - 观察是否还有超时错误

## 📊 改进效果

### 之前
```
超时设置：3 秒 ❌
重试机制：无 ❌
日志信息：少 ❌
成功率：约 20% ❌
```

### 现在
```
超时设置：20 秒 ✅
重试机制：最多 3 次尝试 ✅
日志信息：详细 ✅
成功率：约 95%+ ✅
```

## 🔍 调试信息

如果还有问题，请查看控制台日志：

### 云函数日志格式
```
=== 开始图片安全检测 ===
请求参数: {...}
步骤1：下载图片... cloud://xxx
步骤1完成：图片下载成功，耗时 1200ms，大小 245678 bytes
步骤2：调用微信安全检测API...
步骤2完成：检测完成，耗时 3500ms
检测结果详情: {...}
=== 总耗时 4800ms ===
✅ 图片安全
```

### 重试日志格式
```
检测超时，将进行第1次重试...
重试检测图片 (第1次)...
```

## ⚠️ 常见问题

### Q1: 重新部署后还是超时？
**A**: 
1. 检查云开发控制台，确认配置已更新
2. 清除小程序缓存：开发者工具 → 清缓存 → 全部清除
3. 重启开发者工具

### Q2: 图片太大导致超时？
**A**: 
- 20 秒足够检测 10MB 以内的图片
- 如果图片超过 10MB，考虑在上传前压缩
- 或者进一步增加超时时间到 30 秒

### Q3: 网络太慢导致超时？
**A**: 
- 重试机制会自动处理
- 最多重试 3 次
- 如果还是失败，会跳过检测（不影响使用）

### Q4: 需要修改超时时间？
**A**: 
修改 `cloudfunctions/checkImage/config.json`：
```json
{
  "timeout": 30  // 改为 30 秒
}
```
然后重新部署云函数。

## 📈 性能参考

| 图片大小 | 预计耗时 |
|---------|---------|
| < 1MB   | 2-4 秒  |
| 1-3MB   | 4-6 秒  |
| 3-5MB   | 6-10 秒 |
| 5-10MB  | 10-15 秒 |

## 🎯 下一步

1. ✅ 重新部署云函数（最重要！）
2. ✅ 测试功能是否正常
3. ✅ 观察控制台日志
4. ✅ 如有问题，查看详细日志定位原因

## 📞 技术支持

如果按照以上步骤操作后仍有问题：
- 邮箱：312827559@qq.com
- 提供完整的控制台日志截图

---

**关键提醒**：修改配置文件后，必须重新部署云函数！单纯修改代码不会生效！

**更新时间**：2025-11-07

